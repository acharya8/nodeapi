import express from 'express';
import controller from '../../controllers/admin/banner';

const router = express.Router();

// #region /api/admin/banner/getBanners apidoc
/**
 * @api {post} /api/admin/banner/getBanners Get Bank
 * @apiName Get Bank
 * @apiDescription Get Banners
 * @apiGroup Banners - Admin
 * @apiSuccess (200) {JSON} Result status, message, recordList, totalRecords.
 * @apiSuccessExample {json} Success-200-Response:
 *     HTTP/1.1 200 OK
 *     {
 *          status: 200,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: 'Get Banners',
 *          recordList: Banners,
 *          totalRecords: TotalRecords
 *     }
 * @apiError (500) {JSON} Result message, apiName, apiType, fileName, functionName, lineNumber, typeName, stack.
 * @apiErrorExample {json} Error-500-Response:
 *     HTTP/1.1 500 ERROR
 *     {
 *          status: <error status code>,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: <error message>,
 *          error: {
 *              apiName: <api name>,
 *              apiType: <api type>,
 *              fileName: <file name>,
 *              functionName: <function name>,
 *              lineNumber: <line number>,
 *              typeName: <type name>,
 *              stack: <stack>
 *          },
 *          value: <value if any>
 *     }
 */
// #endregion
router.post('/getBanners', controller.getBanners);

// #region /api/admin/banner/insertBanner apidoc
/**
 * @api {post} /api/admin/banner/insertBanner insert Banner
 * @apiVersion 1.0.0
 * @apiName insert Banner
 * @apiDescription insert Banner
 * @apiGroup Banner - Admin
 * @apiParam  {String}          url                       Requires url of Banner
 * @apiParam  {String}          roleId                Requires roleId for Banner
 * @apiParam  {String}          fromDate               Optional fromDate for Banner
 * * @apiParam  {String}        toDate                  Optional toDate of Banner
 * @apiSuccess (200) {JSON} Result status, message, recordList, totalRecords.
 * @apiSuccessExample {json} Success-200-Response:
 *     HTTP/1.1 200 OK
 *     {
 *          status: 200,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: 'insert Banner',
 *          recordList: Banner,
 *          totalRecords: TotalRecords
 *     }
 * @apiError (500) {JSON} Result message, apiName, apiType, fileName, functionName, lineNumber, typeName, stack.
 * @apiErrorExample {json} Error-500-Response:
 *     HTTP/1.1 500 ERROR
 *     {
 *          status: <error status code>,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: <error message>,
 *          error: {
 *              apiName: <api name>,
 *              apiType: <api type>,
 *              fileName: <file name>,
 *              functionName: <function name>,
 *              lineNumber: <line number>,
 *              typeName: <type name>,
 *              stack: <stack>
 *          },
 *          value: <value if any>
 *     }
 */
// #endregion
router.post('/insertBanner', controller.insertBanner);

// #region /api/admin/banner/activeInactiveBanner apidoc
/**
 * @api {post} /api/admin/banner/activeInactiveBanner Change Banner Status
 * @apiVersion 1.0.0
 * @apiName Change Banner Status
 * @apiDescription Change Banner Status
 * @apiGroup Banner - Admin
 * @apiParam  {Integer}         id                  Requires Banner Id.
 * @apiSuccess (200) {JSON} Result status, message, recordList, totalRecords.
 * @apiSuccessExample {json} Success-200-Response:
 *     HTTP/1.1 200 OK
 *     {
 *          status: 200,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: 'Change Bank Status',
 *          recordList: Bank,
 *          totalRecords: TotalRecords
 *     }
 * @apiError (500) {JSON} Result message, apiName, apiType, fileName, functionName, lineNumber, typeName, stack.
 * @apiErrorExample {json} Error-500-Response:
 *     HTTP/1.1 500 ERROR
 *     {
 *          status: <error status code>,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: <error message>,
 *          error: {
 *              apiName: <api name>,
 *              apiType: <api type>,
 *              fileName: <file name>,
 *              functionName: <function name>,
 *              lineNumber: <line number>,
 *              typeName: <type name>,
 *              stack: <stack>
 *          },
 *          value: <value if any>
 *     }
 */
// #endregion
router.post('/activeInactiveBanner', controller.activeInActiveBanner);

export = router;