import express from 'express';
import controller from '../../controllers/website/contactus';
import header from '../../middleware/apiHeader';

const router = express.Router();

// #region /api/web/contactUs/insertContactUs apidoc
/**
 * @api {post} /api/web/contactUs/insertContactUs Insert Contact Detail
 * @apiVersion 1.0.0
 * @apiName Insert Contact Detail
 * @apiDescription Insert Contact Detail
 * @apiGroup ContactUs - WebSite
 * @apiSuccess (200) {JSON} Result status, message, recordList, totalRecords.
 * @apiSuccessExample {json} Success-200-Response:
 *     HTTP/1.1 200 OK
 *     {
 *          status: 200,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: 'Insert Contact Detail',
 *          recordList: ContactUs,
 *          totalRecords: TotalRecords
 *     }
 * @apiError (500) {JSON} Result message, apiName, apiType, fileName, functionName, lineNumber, typeName, stack.
 * @apiErrorExample {json} Error-500-Response:
 *     HTTP/1.1 500 ERROR
 *     {
 *          status: <error status code>,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: <error message>,
 *          error: {
 *              apiName: <api name>,
 *              apiType: <api type>,
 *              fileName: <file name>,
 *              functionName: <function name>,
 *              lineNumber: <line number>,
 *              typeName: <type name>,
 *              stack: <stack>
 *          },
 *          value: <value if any>
 *     }
 */
// #endregion
router.post('/insertContactUs', controller.insertContactUs);

// #region /api/web/contactUs/insertNewsLetterSubscription apidoc
/**
 * @api {post} /api/web/contactUs/insertNewsLetterSubscription Register Email For News
 * @apiVersion 1.0.0
 * @apiName Register Email For News
 * @apiDescription Register Email For News
 * @apiGroup ContactUs - WebSite
 * @apiParam {string}           email           Required email Id
 * @apiSuccess (200) {JSON} Result status, message, recordList, totalRecords.
 * @apiSuccessExample {json} Success-200-Response:
 *     HTTP/1.1 200 OK
 *     {
 *          status: 200,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: 'Register Email For News',
 *          recordList: ContactUs,
 *          totalRecords: TotalRecords
 *     }
 * @apiError (500) {JSON} Result message, apiName, apiType, fileName, functionName, lineNumber, typeName, stack.
 * @apiErrorExample {json} Error-500-Response:
 *     HTTP/1.1 500 ERROR
 *     {
 *          status: <error status code>,
 *          isDisplayMessage: <true/false>, // if the value is true then display message on screen
 *          message: <error message>,
 *          error: {
 *              apiName: <api name>,
 *              apiType: <api type>,
 *              fileName: <file name>,
 *              functionName: <function name>,
 *              lineNumber: <line number>,
 *              typeName: <type name>,
 *              stack: <stack>
 *          },
 *          value: <value if any>
 *     }
 */
// #endregion
router.post('/insertNewsLetterSubscription', controller.insertNewsLetterSubscription);

export = router;